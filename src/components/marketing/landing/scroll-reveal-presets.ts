/** Shared ScrollReveal specs for the Veraz landing (React Bits customize). */
export const LANDING_SCROLL_REVEAL = {
  enableBlur: true,
  blurStrength: 4,
  baseOpacity: 0.1,
  baseRotation: 3,
} as const;
