/** Conceptual value objects — no validation logic yet. */

export type Url = string;
export type Slug = string;
export type Instant = string; // ISO-8601 in domain docs; typed later if needed
export type LocaleCode = string;
export type IsoCountryCode = string;
